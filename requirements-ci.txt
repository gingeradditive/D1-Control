# Sottoinsieme di requirements.txt per CI e sviluppo desktop: esclude i
# pacchetti hardware specifici del Raspberry Pi (RPi.GPIO, spidev, pigpio).
# Il codice li importa già in un try/except e ricade in modalità simulazione
# quando mancano (vedi backend/dryer/components/*.py, backend/network/scanner.py);
# su un runner non-ARM quei pacchetti non si builderebbero comunque.

# Web framework
fastapi==0.116.1
starlette==0.47.2
python-multipart==0.0.20

# ASGI server
uvicorn==0.35.0
uvloop==0.21.0
httptools==0.6.4
h11==0.16.0

# Pydantic
pydantic==2.11.7
pydantic_core==2.33.2
annotated-types==0.7.0
typing-inspection==0.4.1
typing_extensions==4.14.1

# HTTP client
requests==2.32.4
httpx==0.28.1
httpcore==1.0.9
certifi==2025.7.14
charset-normalizer==3.4.2
idna==3.10
urllib3==2.5.0

# Async
anyio==4.9.0
sniffio==1.3.1
click==8.2.1

pytest==8.4.2
